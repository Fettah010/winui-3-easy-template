# System tray

This scaffold includes tray minimize, autostart, desktop notifications, and
the related service and tests. Review `Services/SystemTrayService.cs` and
`Services/AutoStartService.cs` before changing minimize behavior.
