# Session State — mutable, update every session

> Agents: read `AGENTS.md` → this file → `docs/DISCOVERY-PLAN.md`, then run
the bootstrap checklist in `AGENTS.md`. Refresh this file before ending
the session (goal, tree, CI, next). Conventions live in `AGENTS.md`;
this file holds only **current facts**.

- **Goal:** continue template discoverability and SEO improvements tracked in `docs/DISCOVERY-PLAN.md`.
- **Version:** app `0.0.2-beta`; template package `0.1.5`.
- **Tags:** `v0.0.2-beta` and `templates-v0.1.5` shipped.
- **Branches:** `main` contains the published template-reusability release plus the restored discovery work; `beta` tracks the release commit.
- **Tree:** discovery plan and related metadata changes restored locally and ready to publish.
- **CI health (last known):** local build 0 warnings/0 errors, unit tests 131/131, scaffold matrix passed, mirror parity passed; release workflows for v0.0.2-beta and templates-v0.1.5 passed.
- **Next:** continue the checked discovery-plan phase, then verify and release the next version.
- **Open questions:** none blocking.