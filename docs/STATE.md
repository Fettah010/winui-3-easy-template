# Session State — mutable, update every session

> Agents: read `AGENTS.md` → this file → `docs/FLEXIBILITY-PLAN.md`, then run
the bootstrap checklist in `AGENTS.md`. Refresh this file before ending
the session (goal, tree, CI, next). Conventions live in `AGENTS.md`;
this file holds only **current facts**.

- **Goal:** flexibility plan shipped — pick next work (discovery plan or roadmap triage).
- **Version:** app `0.0.4-beta`; template package `0.2.0`.
- **Tags:** `v0.0.4-beta` and `templates-v0.2.0` shipped and CI-green.
- **Branches:** `main` @ `0abf7e2`; `beta` tracks `v0.0.4-beta`.
- **Tree:** clean — Phases 0–4 committed (`00f50bf`) + release commit (`0abf7e2`), all pushed.
- **CI health (last known):** GitHub Release `v0.0.4-beta` published (Velopack beta channel); NuGet `DevTem.Templates` 0.2.0 live; release + templates-publish + msix + templates-matrix (15 combos) all green in Actions.
- **Next:** next work TBD. Open: `DISCOVERY-PLAN.md` A3/A4+B/C/D, roadmap triage (85 boxes), add-page for en-only scaffolds.
